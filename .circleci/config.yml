version: 2
jobs:
  build:
    machine:
      - image: circleci/classic:edge
    parallelism: 2

    steps:
      - checkout
      - run: command: echo 127.0.0.1 devhost | sudo tee -a /etc/hosts
      - run: command: ls
      - run:
          name: Run build script
          command: ./build
          working_directory: ~/armhf-alpinelinux
