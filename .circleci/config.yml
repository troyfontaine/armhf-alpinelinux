version: 2
jobs:
  build:
    machine:
      - image: circleci/classic:edge
    parallelism: 1

    steps:
      - run: command: ls
      - run:
          name: Run build script
          command: ./build
          working_directory: ~/
