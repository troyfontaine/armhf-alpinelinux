version: 2
jobs:
  build:
    machine:
      - image: circleci/classic:edge
    parallelism: 1
    branches:
      ignore:

    steps:
      - checkout

      - run:
          name: Run build script
          command: ./build
          working_directory: ~/
